# Agent Skills 目录结构构建 强制执行规则
（Agent 构建/修改 Skills 目录时，**必须逐条严格遵守**，不可违反）

## 一、核心总规则
1. 严格遵循 **三层架构物理隔离**，禁止跨层混用、目录嵌套混乱
2. 严格执行 **统一命名规范**，禁止自定义命名、随意缩写
3. 严格保证 **根目录唯一入口**，禁止在根目录新增无关文件/文件夹
4. 严格遵守 **即插即用** 原则，新增能力零侵入原有体系

---
## 二、目录层级规则（固定不可修改）
```
skills/
├─ SKILL.md（唯一顶层路由）
├─ common/        🔹 L1 公共基础层
├─ products/      🔹 L2 产品能力层
└─ orchestration/ 🔹 L3 业务编排层
```
1. 仅允许存在以上 **4 个根级对象**，禁止新增/删除根目录
2. 三层目录**互相独立**，禁止在 common 中写业务代码，禁止在 products 中写编排逻辑

---

## 三、标准化命名规则（强制）
### 1. 文件夹命名
- 产品文件夹：**全小写英文**（如：`product-name/`）
- 编排场景文件夹：**小写+下划线**（如：`one-click-video-creation/`、`log-analysis/`）

### 2. 文件命名
- 顶层路由：`SKILL.md`（固定大写）
- 产品手册：**与文件夹同名** `{产品目录名}_SKILL.md`（例：`my-api-product_SKILL.md`）
- 编排手册：**与场景文件夹同名** `{场景目录名}_SKILL.md`（例：`my-cross-product-flow_SKILL.md`）
- 可执行代码：**一律**放在 `scripts/` 下（含各产品的 `cli_capabilities.py` 与 CLI 脚本；L3 场景同理）

---

## 四、各层文件内容规则
### 🔹 L1 common/（公共基础层）
- 仅允许存放：`base.py`、`exceptions.py`、`logger.py`
- 用途：通用工具、异常、日志；HTTP 客户端、鉴权、轮询等**非产品业务**能力可**合并进 `base.py`**，以满足「仅三文件」约束。**禁止**写入任何产品领域业务逻辑。
- 所有产品可复用，禁止修改底层代码破坏上层约定

### 🔹 L2 products/（产品能力层）
- 每个产品 = 独立文件夹 + **1 个手册文件 + 1 个脚本目录**
  1. `{产品目录名}_SKILL.md`：产品操作手册（场景、示例、返回结构）
  2. `scripts/`：可执行脚本 + **`cli_capabilities.py`**（机器可读能力目录）
- **禁止**在产品目录根散落其它 `.py` 可执行入口（须在 `scripts/` 内）
- 禁止一个文件夹存放多个产品能力

### 🔹 L3 orchestration/（编排层）
- 每个业务场景 = 独立文件夹 + **仅 1 个** `{场景目录名}_SKILL.md`
- 编写组合逻辑，必须调用 `products/` 中的能力；**可**添加 `scripts/`、`templates/`、`README.md`（补充说明，**不**作为第二份路由手册）

---

## 五、SKILL.md 路由规则
1. **根目录 SKILL.md**：以**路由与层级索引**为主，**禁止**内嵌可执行代码或逐步业务算法；**须**包含 **「运行时契约」**（见 **§六**），使「入口文档」与「实际调用 `scripts/`」的行为一致。
2. **产品 SKILL.md**：仅描述当前产品的能力、示例、参数
3. **编排 SKILL.md**：仅描述跨产品组合流程
4. 禁止在路由文档中堆砌与执行无关的冗长示例；**与网络、凭据、落盘、子进程相关的声明不得缺失**（见 **§六**）。

---

## 六、运行时契约与注册元数据（含 `scripts/` 的技能包强制执行）

**本节只规定「文档化义务」与结构要点**：**不**写入任何具体厂商 API、环境变量名、凭证路径、成片文件名或业务参数——**业务真值**仅允许出现在各技能包根 **`SKILL.md`**、**`products/*/…_SKILL.md`**、**`orchestration/*/…_SKILL.md`** 及实现代码中。

凡包内含 **`products/*/scripts/`** 或 **`orchestration/*/scripts/`**，且脚本会 **访问外网**、**读写磁盘**（含配置/缓存/产物）、或 **拉起子进程**（含外部 CLI），**根目录 `SKILL.md` 必须**包含 **「运行时契约」**，并与 frontmatter **`description`** 一致，至少覆盖下列**类别**（按本包实际行为填写具体内容）：

### 1. 前置元数据（YAML frontmatter）
- **`always`**：默认或显式为 **`false`**（**不强制**随会话默认加载；由用户/宿主按需启用），除非宿主规范另有规定。
- **`description`**：除路由说明外，须用**概括性**语句列出真实副作用类别（例如：出站 API、本地凭据或配置读写、下载、子进程/外部二进制、浏览器引导等），并指向篇内「运行时契约」。

### 2. 正文「运行时契约」须覆盖的类别（不规定具体键名）
- **身份与密钥**：是否存在本地配置文件路径、是否读写过期时间/令牌刷新、默认 API 基址（若适用）；**具体路径与变量名由包内文档写明**。
- **环境变量**：凡代码读取的、影响路径/基址/行为上限的变量，须在契约中列出 **默认值、是否必需、用途**；**不得**在注册描述中写「无依赖」而实现侧实际读取环境变量。
- **外部二进制**：若脚本依赖 **`PATH`** 中可执行文件（媒体工具链、CLI 等），须声明**何种脚本路径需要**、何种**仅 API** 不需要。
- **副作用分类**：对 **HTTPS**、**CDN/大文件下载**、**子进程**、**浏览器**、**工作目录/输出目录约定** 等分类说明，并标明**与哪些脚本或场景相关**（避免「一刀切」与实现不符）。

### 3. 持久性变更与用户可控性（须单独成节或等价表格）
- **凭据与密钥材料**：用户如何改路径、轮换、删除；**禁止**将密钥纳入版本库（在包内文档与示例中重申）。
- **产物与中间文件**：命令行参数或环境变量如何指定输出根目录；默认相对 **cwd** 落盘时的含义；**具体文件名与目录结构由场景手册说明**，本 rule 不规定。
- **边界声明**：默认**不得**将持久化写入 **其它技能包目录**、**`.cursor/rules`**、编辑器**全局 Agent/用户配置**等——除非该产品**明确**以「写用户配置」为能力且**单独成章**说明；新增脚本时保持边界一致。

### 4. 编排与契约文档交叉引用
- 存在 **`orchestration/CONTRACT_SKILL.md`** 时，其「执行」步骤须指向包根 **`SKILL.md`「运行时契约」**及持久化说明。
- **`orchestration/README.md`** 须提醒：执行 `scripts/` 前阅读上述契约。

### 5. 维护义务
- 新增或变更会 **写盘 / 改凭据 / 拉子进程 / 改默认输出路径** 的脚本时，**同步更新**根 **`SKILL.md`「运行时契约」**与 **`description`**，避免元数据与实现不一致。

---

## 七、禁止行为（红线规则）
1. ❌ 禁止在根目录创建额外文件夹/文件
2. ❌ 禁止修改三层目录名称、结构
3. ❌ 禁止自定义文件命名（必须按规则执行）
4. ❌ 禁止在公共层写入业务代码
5. ❌ 禁止在产品层实现编排能力

---

## 八、新增能力标准流程
1. 新增产品 → 在 `products/` 新建产品文件夹 → 添加标准文件
2. 新增编排场景 → 在 `orchestration/` 新建场景文件夹 → 添加标准文件
3. 新增通用工具 → 仅修改 `common/` 下文件
4. 完成后更新根目录 `SKILL.md` 路由
5. 若新增/变更 **`scripts/`** 行为涉及 **§六**（外网、凭据、落盘、子进程），**同步更新**根 **`SKILL.md`「运行时契约」**与 **`CONTRACT_SKILL.md` 等交叉引用
6. 全程不修改、不删除原有文件（**除非**用户明确要求重构或该文件违反本规则）