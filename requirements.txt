
# ---------------------------------------------------------------------------
# ffmpeg / ffprobe —— 需要，但不是 pip 包
# ---------------------------------------------------------------------------
# run_render.py 等用 subprocess 调用命令行里的 ffmpeg、ffprobe（见 require_bin）。

# 执行: pip install -r requirements.txt）
ruff>=0.8.0
pytest>=8.0.0

